# ポリシーサーバーの依存（必須、削除しないでください）
fastapi>=0.68
uvicorn>=0.15
msgpack>=1.0
numpy>=1.19

# 以下に自分のモデルに必要なライブラリを追加してください
# 例:
# torch>=2.0
# transformers>=4.30

# Pi0 (lerobot) 推論用
# policy_server.py は siglip_patch.py で lerobot / transformers の内部実装に
# モンキーパッチを当てているため、バージョンがずれるとパッチが噛み合わなくなる。
# ローカルで疎通確認したバージョンに固定する。
torch==2.10.0
transformers==4.57.6
lerobot[transformers-dep]==0.4.4
